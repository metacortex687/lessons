```
1. Пример максимально раннего связывания, в python это всеравно при выполнении:
 
DiscourseDataExplorerApi:
	__init__(self)
		self.max_connection_attempts = 3

#Связываю при инициализации класса. 
#Вариант с использованием константы, не дает лучшего понимания кода. Смысл переменной понятен из контекста.
#В вынесения в переменные окружения или константы, вроде в этом нет необходимости.


2. Связывание в момент выполнения. Значение берется из переменных окружения. Это дает гибкость настройки.     
	   posthog_query = PostHogQuery(
        api_key=os.getenv("POSTHOG_PERSONAL_API_KEY"),
        project_id=os.getenv("POSTHOG_PROJECT_ID"),
        base_url=os.getenv("POSTHOG_BASE_URL")
    )
    
    
3. Связывание  с использованием константы

   COLOR_RED = (255,0,0)
	
    Agent:
		__init__(self)
			self._color = COLOR_RED

#Связывание с использованием константы, так как это улучшает понимание кода. Избегаю магического кортежа.

```