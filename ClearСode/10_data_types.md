```
1. Работа со строками
Было:
сообщение = "Обработка выполнена"  

Стало:
сообщение = НСтр(“ru='Обработка выполнена')

//рекомендованный в 1C способ работы со строками для поддержки мультиязычности. Платформа соберет все эти строки в одну таблицу, где можно будет сопоставить им переводы на другие языки


2. Использую логические переменные. 
finished = turn == SIMULATION_DURATION_TURNS

3. Использую логические переменные
first_turn = turn == 0

4. Использую логические переменные
is_input_layer =  i == 0

5. Использую логические переменные
is_output_layer =  i == count_layer

6. Использую логические переменные. 
is_all_agent_die = count_agents == 0

7. Использую логические переменные
is_outborder_right = x >= MAP_WIDTH 

8. Использую логические переменные
is_outborder_left= x < 0

9. Использую логические переменные
is_outborder_bottom = y >= MAP_HEIGHT  

10. Использую логические переменные
is_outborder_down = y < 0

11. Избегайте сравнений на равенство вещественных чисел.
    //В unittest после деления, заменил сравнение на равенство assertEqual на проверку попадания в интервал 
self.assertAlmostEqual(value, expected, delta=0.0001)

    
12. Разработайте стратегию интернационализации/локализации текстовых сообщений в вашем коде
//Буду использовать библиотеку для поддержки мультиязычности "gettext" в проекте на Python
//пример кода вместо обычного присвоения занчения в строковую переменную:

import gettext
gettext.bindtextdomain('myapp', 'locale')
gettext.textdomain('myapp')
_ = gettext.gettext
welcome_text = _("Welcome")
```