```

1) Группировка команд.
   в классе PostHogWriter
   в методе write_events
   вынес поиск уже синхронизированных событий в приватный метод _find_events_by_sync_key

2) Группировка команд.
   в методе send_posthog_query
   решил не возвращать путь endpoin как одно из значение кортежа, то-есть увеличивать размер команды 
   а седелать отдельный метод
   path_posthog_query

3) Группировка команд.
   В классе AisystantApi
   вынес в приватный метод _login() получение токена для совершения последующих запросов
   

4) Уменьшение области видимости переменной.
   переменную 
   self.cache - self._cache
   //Сделал приватной

5) Уменьшение области видимости переменной.
   self.token - self._token
   //Сделал приватной

6) Уменьшение области видимости переменной.
   перменной.
   self.base_url - self._base_url
   //Сделал приватной

7) Уменьшение области видимости переменной.
   self.username - self._username
   //Сделал приватной

8) Уменьшение области видимости переменной.
   self.password - self._password
   //Сделал приватной

9) Уменьшение области видимости переменной.
   self.delay - self._delay
   //Сделал приватной

10) Уменьшение области видимости переменной.
    self.session - self._session 
    //Сделал приватной
    
В другом классе DiscourseDataExplorerApi переменовал некоторые одноименные переменные. И дополнительно,
    
11) Уменьшение области видимости перменной.
    self.api_key  -  self._api_key 
    //Сделал приватной

12) Уменьшение области видимости перменной.
	self.api_username  - self._api_username
    //Сделал приватной

13) Уменьшение области видимости перменной.
	self.verify_ssl - self._verify_ssl
    //Сделал приватной

14) Группировка команд.
    В классе CommentsApi из метода get_events
    извлек команды в метод _get_raw_data и сделал его приватным 

15) Группировка команд.
    в методе get_sorted_awardable_events_by_user класса SortedEventsForAward 
    получение запроса вынес в отдельный метод _sql_query, в результате метод стал визуально проще 
    

```